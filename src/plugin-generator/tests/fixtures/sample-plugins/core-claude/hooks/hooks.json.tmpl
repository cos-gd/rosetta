{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [{{{bootstrap_hooks_claude}}}]
      }
    ]{{#if deterministic_hooks}},{{/if}}
{{#if deterministic_hooks}}
    "PreToolUse": []
{{/if}}
  }
}
